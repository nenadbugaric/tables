import namor from 'namor'const range = len => {  const arr = []  for (let i = 0; i < len; i++) {    arr.push(i)  }  return arr}const newRecord = (i) => {  return {    firstName: namor.generate({ words: 1, numbers: 0 }),    lastName: namor.generate({ words: 1, numbers: 0 }),    age: Math.floor(Math.random() * 30),    visits: Math.floor(Math.random() * 100),    progress: Math.floor(Math.random() * 100),    uuid: `${Math.floor(Math.random() * 100000000)}-${Math.floor(Math.random() * 100000000)}}`,    chargeName: "Vorverkaufsgebühr",    chargeCode: "VVG-C, 4",    priceType: i % 3 === 0 ? "KIND, Kind" : "SCHU, Schüler",    pk1: Math.floor(Math.random() * 10000),    pk2: Math.floor(Math.random() * 10000),}};function makeData(...lens) {  const makeDataLevel = (depth = 0) => {    const len = lens[depth]    return range(len).map(i => {      return {        ...newRecord(i),        // subRows: lens[depth + 1] ? makeDataLevel(depth + 1) : undefined,      }    })  }  return makeDataLevel()}console.time('dataset');export const rows = makeData(500);console.timeEnd('dataset');console.log(rows.length);